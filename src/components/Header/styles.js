import styled from 'styled-components'

export const Container = styled.nav`
  padding: 48px;
  text-align: center;
  top: 0;

  @media (max-width: 720px) {
    text-align: left;
    display: block;
    padding: 2rem;
  }
`
